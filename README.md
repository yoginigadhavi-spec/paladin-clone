# Paladin Clone

Standalone React + Babel version running locally.

## Setup

```bash
rm -rf node_modules package-lock.json
npm install
npm run dev
```

The development server will start on `http://localhost:3000`

## Build

```bash
npm run build
```
